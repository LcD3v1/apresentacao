import pt from './pt';
import en from './en';
import ar from './ar';

export const translations = { pt, en, ar };

/** Ordem exibida no seletor de idioma. */
export const languageCodes = ['pt', 'en', 'ar'];

export const DEFAULT_LANGUAGE = 'pt';

export default translations;
